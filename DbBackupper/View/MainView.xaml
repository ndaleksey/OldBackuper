<Window x:Class="Swsu.Tools.DbBackupper.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:Swsu.Tools.DbBackupper.ViewModel"
        xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:model="clr-namespace:Swsu.Tools.DbBackupper.Model"
        xmlns:service="clr-namespace:Swsu.Tools.DbBackupper.Service"
        mc:Ignorable="d"
        Title="Генератор резервных копий" Height="700" Width="900" MinWidth="900" MinHeight="700">

    <Window.Resources>
        <!-- *********************************************************************************************************** -->

        <!-- *********************************************************************************************************** -->
        <!-- ***************************************** GroupBox ******************************************************** -->
        <BorderGapMaskConverter x:Key="BorderGapMaskConverter"/>
        <Style x:Key="GeneralGroupBox" TargetType="{x:Type GroupBox}">
            <Setter Property="BorderBrush" Value="Teal"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupBox}">
                        <Grid SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="6"/>
                                <ColumnDefinition Width="54.167"/>
                                <ColumnDefinition Width="12.487"/>
                                <ColumnDefinition Width="23*"/>
                                <ColumnDefinition Width="38*"/>
                                <ColumnDefinition Width="6"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="6"/>
                            </Grid.RowDefinitions>
                            <Border BorderBrush="Transparent" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.ColumnSpan="6" Grid.Column="0" CornerRadius="4" Grid.Row="1" Grid.RowSpan="3" Margin="0"/>
                            <Border BorderBrush="White" BorderThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="6" CornerRadius="4" Grid.Row="1" Grid.RowSpan="3" Margin="0">
                                <Border.OpacityMask>
                                    <MultiBinding ConverterParameter="7" Converter="{StaticResource BorderGapMaskConverter}">
                                        <Binding ElementName="Header" Path="ActualWidth"/>
                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource Self}"/>
                                        <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}"/>
                                    </MultiBinding>
                                </Border.OpacityMask>
                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                                    <Border BorderBrush="White" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2"/>
                                </Border>
                            </Border>
                            <Border x:Name="Header" BorderThickness="1" BorderBrush="#FF000CFF" Grid.Column="1" Padding="3,1,3,0" Grid.Row="0" Grid.RowSpan="2" Background="#FF5BF0E9" Grid.ColumnSpan="4" Margin="0,0,0.001,0" CornerRadius="4">
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ContentPresenter Grid.ColumnSpan="4" Grid.Column="1" Margin="0, 5" Grid.Row="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ***************************************** TextBox ******************************************************** -->
        <SolidColorBrush x:Key="TextBox.Static.Border" Color="BlueViolet"/>
        <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#FF7EB4EA"/>
        <SolidColorBrush x:Key="TextBox.Focus.Border" Color="#FF569DE5"/>

        <Style x:Key="ConnectionTextBox" TargetType="{x:Type TextBox}">
            <!--            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>-->
            <Setter Property="Foreground" Value="DarkBlue"/>
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="BorderBrush" Value="{StaticResource TextBox.Static.Border}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="AllowDrop" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" CornerRadius="4" VerticalAlignment="Center" BorderBrush="{TemplateBinding BorderBrush}" 
        					BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true"/>
                        <Condition Property="IsSelectionActive" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <!--<Style TargetType="TreeView">
            <Setter Property="Background" Value="GhostWhite"/>
            <Setter Property="Foreground" Value="GhostWhite"></Setter>
        </Style>-->

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="3, 3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="25"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <viewModel:MainViewModel x:Name="viewModel"/>
    </Window.DataContext>

    <dxmvvm:Interaction.Behaviors>
        <service:ListBoxService Control="{Binding ElementName=RestoreLogsListBox}" x:Name="RestoreLogsListBoxService"/>
        <service:ListBoxService Control="{Binding ElementName=DumpLogsListBox}" x:Name="DumpLogsListBoxService"/>

    </dxmvvm:Interaction.Behaviors>

    <TabControl Background="Beige" >
        <TabItem Header="Создать резервную копию БД">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Vertical">
                    <GroupBox Header="Подключение">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Label Content="Хост:" Margin="0, 5"/>
                            <Grid Grid.Row="0" Grid.Column="1" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!--                                <xctk:MaskedTextBox Text="{Binding Path=DumpConnectionStringBuilder.Host, Mode=TwoWay}" Mask="000.000.000.000" Margin="0, 5"/>-->
                                <TextBox Text="{Binding DumpConnectionStringBuilder.Host, Mode=TwoWay}" Margin="0, 5"/>
                                <Button Grid.Column="1" Content="PING" Height="20" Margin="5, 0, 0, 0" Command="{Binding DumpPingCommand}"/>
                            </Grid>

                            <Label Grid.Row="1" Grid.Column="0" Content="Порт:" Margin="0, 5"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DumpConnectionStringBuilder.Port}" Margin="0, 5"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="Имя БД:" Margin="0, 5"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DumpConnectionStringBuilder.Database}" Margin="0, 5"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="Имя пользователя:" Margin="0, 5"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding DumpConnectionStringBuilder.UserId}" Margin="0, 5"/>

                            <Label Grid.Row="4" Grid.Column="0" Content="Пароль:" Margin="0, 5"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding DumpConnectionStringBuilder.Password}" Margin="0, 5"/>

                            <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Content="Получить структуру БД" Command="{Binding GetSchemesForDumpCommand}" Margin="5, 5"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Формат файла">
                        <GroupBox.Resources>
                            <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="FileFormatValues">
                                <ObjectDataProvider.MethodParameters>
                                    <x:Type TypeName="model:FileFormat" />
                                </ObjectDataProvider.MethodParameters>
                            </ObjectDataProvider>
                        </GroupBox.Resources>

                        <ComboBox ItemsSource="{Binding Source={StaticResource FileFormatValues}}" SelectedItem="{Binding DumpFileFormat}" Margin="5, 5"/>
                    </GroupBox>

                    <GroupBox Header="Файл">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Расположение:" VerticalAlignment="Center" Margin="5, 5"/>
                            <TextBox Grid.Column="1" Text="{Binding OutDumpFileName}" IsReadOnly="True" Margin="5, 5"/>
                            <Button Grid.Column="2" Width="30" Content=" . . . " Command="{Binding SaveDialogCommand}" Margin="5, 5"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Типы объектов">
                        <StackPanel Orientation="Vertical">
                            <CheckBox x:Name="OnlyDataCheckBox1" Content="Только данные" IsChecked="{Binding OnlyDumpData}" Margin="5, 5">
                                <CheckBox.Style>
                                    <Style TargetType="{x:Type CheckBox}">
                                        <Setter Property="IsEnabled" Value="True"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsChecked, ElementName=OnlySchemeCheckBox1}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <CheckBox x:Name="OnlySchemeCheckBox1" Content="Только схема" IsChecked="{Binding OnlyDumpScheme}" Margin="5, 5">
                                <CheckBox.Style>
                                    <Style TargetType="{x:Type CheckBox}">
                                        <Setter Property="IsEnabled" Value="True"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsChecked, ElementName=OnlyDataCheckBox1}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                            <CheckBox Content="Blobs" IsChecked="{Binding IsBlobs}" Margin="5, 5"/>
                        </StackPanel>
                    </GroupBox>

                    <Button Content="Создать резервную копии" Command="{Binding MakeBackupCommand}" Margin="15, 15"/>
                </StackPanel>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="30*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Header="Схемы:">
                        <TreeView ItemsSource="{Binding DumpSchemes}" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                                    <HierarchicalDataTemplate.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                                        </DataTemplate>
                                    </HierarchicalDataTemplate.ItemTemplate>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </GroupBox>

                    <GridSplitter Grid.Row="1" Height="5" BorderThickness="1" Background="#5E7357"  HorizontalAlignment="Stretch"/>

                    <GroupBox Grid.Row="2" Header="События:">
                        <ListBox x:Name="DumpLogsListBox" MinHeight="200" ItemsSource="{Binding DumpLogs}" IsSynchronizedWithCurrentItem="True" >
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                        </ListBox>
                    </GroupBox>
                </Grid>
            </Grid>
        </TabItem>

        <TabItem Header="Восстановить БД из резервной копии">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Vertical">
                    <GroupBox Header="Подключение">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Label Content="Хост:" Margin="0, 5"/>
                            <Grid Grid.Row="0" Grid.Column="1" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!--                                <xctk:MaskedTextBox Text="{Binding Path=DumpConnectionStringBuilder.Host, Mode=TwoWay}" Mask="000.000.000.000" Margin="0, 5"/>-->
                                <TextBox Text="{Binding RestoreConnectionStringBuilder.Host, Mode=TwoWay}" Margin="0, 5"/>
                                <Button Grid.Column="1" Content="PING" Height="20" Margin="5, 0, 0, 0" Command="{Binding RestorePingCommand}"/>
                            </Grid>

                            <Label Grid.Row="1" Grid.Column="0" Content="Порт:" Margin="0, 5"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding RestoreConnectionStringBuilder.Port}" Margin="0, 5"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="Имя БД:" Margin="0, 5"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RestoreConnectionStringBuilder.Database}" Margin="0, 5"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="Имя пользователя:" Margin="0, 5"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding RestoreConnectionStringBuilder.UserId}" Margin="0, 5"/>

                            <Label Grid.Row="4" Grid.Column="0" Content="Пароль:" Margin="0, 5"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding RestoreConnectionStringBuilder.Password}" Margin="0, 5"/>

                            <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Content="Получить структуру БД" Command="{Binding GetSchemesForRestoreCommand}" Margin="5, 5"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Header="Формат файла">
                        <GroupBox.Resources>
                            <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="FileFormatValues">
                                <ObjectDataProvider.MethodParameters>
                                    <x:Type TypeName="model:FileFormat" />
                                </ObjectDataProvider.MethodParameters>
                            </ObjectDataProvider>
                        </GroupBox.Resources>

                        <ComboBox ItemsSource="{Binding Source={StaticResource FileFormatValues}}" SelectedItem="{Binding RestoreFileFormat}" Margin="5, 5"/>
                    </GroupBox>

                    <GroupBox Header="Файл">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Расположение:" Margin="5, 5"/>
                            <TextBox Grid.Column="1" Text="{Binding InDumpFileName}" IsReadOnly="True" Margin="5, 5"/>
                            <Button Grid.Column="2" Width="30" Content=" . . . " Command="{Binding OpenDialogCommand}" Margin="5, 5"/>
                        </Grid>
                    </GroupBox>


                    <GroupBox Header="Типы объектов">
                        <StackPanel Orientation="Vertical">
                            <CheckBox x:Name="OnlyDataCheckBox2" Content="Только данные" IsChecked="{Binding OnlyRestoreData}"  Margin="5, 5">
                                <CheckBox.Style>
                                    <Style TargetType="{x:Type CheckBox}">
                                        <Setter Property="IsEnabled" Value="True"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsChecked, ElementName=OnlySchemeCheckBox2}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <CheckBox x:Name="OnlySchemeCheckBox2" Content="Только схема" IsChecked="{Binding OnlyRestoreScheme}"  Margin="5, 5">
                                <CheckBox.Style>
                                    <Style TargetType="{x:Type CheckBox}">
                                        <Setter Property="IsEnabled" Value="True"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsChecked, ElementName=OnlyDataCheckBox2}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                        </StackPanel>
                    </GroupBox>

                    <Button Content="Восстановить из резервной копии" Command="{Binding RestoreBackupCommand}" Margin="5, 15"/>
                </StackPanel>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <GroupBox Header="Схемы:">
                        <TreeView ItemsSource="{Binding RestoreSchemes}" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                                    <HierarchicalDataTemplate.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                                        </DataTemplate>
                                    </HierarchicalDataTemplate.ItemTemplate>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </GroupBox>

                    <GridSplitter Grid.Row="1" Height="5" BorderThickness="1" Background="#5E7357"  HorizontalAlignment="Stretch"/>

                    <GroupBox Grid.Row="2" Header="События:">
                        <ListBox x:Name="RestoreLogsListBox" Height="300" ItemsSource="{Binding RestoreLogs}" IsSynchronizedWithCurrentItem="True" Margin="0,3,0,-3" >
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                        </ListBox>
                    </GroupBox>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>

</Window>
